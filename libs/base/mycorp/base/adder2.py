"""Example module of the base library"""


def add2(x: int, y: int) -> int:
    """
    Adds two integers

    Args:
        x: the left operand
        y: the right operand
    Returns:
        The sum of x and y
    """
    return x + y
